using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Left : MonoBehaviour
{
   /* // Start is called before the first frame update
    PlayerController playCont;
    rPathsBlack rPaths;
    // Start is called before the first frame update
    void Start()
    {
        playCont = GameObject.Find("Character1").GetComponent<PlayerController>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void OnTriggerEnter2D(Collider2D path)
    {
        if(path.tag == "Path")
        {
           rPaths = path.GetComponent<rPathsBlack>();
           if(rPaths.closed == false)
           {
                playCont.left = true;
           }
           else
           {
                playCont.left = true;
           }
        }
    }
    void OnTriggerStay2D(Collider2D path)
    {
        if(path.tag == "Path")
        {
           rPaths = path.GetComponent<rPathsBlack>();
           if(rPaths.closed)
           {
                playCont.left = true;
           }
           else
           {
                playCont.left = true;
           }
        }
    }*/
}
